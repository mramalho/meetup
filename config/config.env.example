# Configuração para create-all.sh e destroy-all.sh
# Copie para config.env e preencha os valores:
#   cp config/config.env.example config/config.env
#   nano config/config.env

# === Obrigatórios ===

# Domínio do site (ex: example.com)
DOMAIN_NAME="example.com"

# ID da Hosted Zone no Route53 para o domínio.
# Deixe vazio para tentar descobrir automaticamente (script busca zona que contém o domínio).
# Liste zonas: aws route53 list-hosted-zones --query "HostedZones[*].{Name:Name,Id:Id}" --output table
HOSTED_ZONE_ID=""

# === Opcionais - Recursos AWS ===

# Criar certificado ACM via AWS CLI? (1=sim, 0=não - use certificado existente em ACM_CERTIFICATE_ARN)
CREATE_ACM=1

# ARN do certificado ACM (us-east-1). Preenchido automaticamente se CREATE_ACM=1.
# Se CREATE_ACM=0, defina manualmente o ARN de um certificado já validado.
ACM_CERTIFICATE_ARN=""

# Criar usuário IAM para deploy? (1=sim, 0=não)
CREATE_IAM_USER=0

# Nome do usuário IAM (se CREATE_IAM_USER=1)
DEPLOY_USER_NAME="iam-deploy"

# === Opcionais - Terraform ===

AWS_REGION="us-east-2"

# Token de acesso à página (deixe vazio para acesso livre). Gere com: openssl rand -hex 24
ACCESS_TOKEN=""

# Origens extras para CORS (se upload falhar com erro de conexão). Ex: http://localhost:8080
CORS_EXTRA_ORIGINS=""
BUCKET_NAME="your-bucket-name"

# Bedrock
BEDROCK_REGION="us-east-2"
BEDROCK_MODEL_ID="anthropic.claude-opus-4-6-v1"
BEDROCK_INFERENCE_PROFILE=""

# Retenção dos logs do Bedrock no CloudWatch (dias). 0 = indefinido
BEDROCK_LOGS_RETENTION_DAYS=30

# Observabilidade (feature flags para troubleshooting)
# OBSERVABILITY_DEBUG=1  -> log evento completo e respostas API no CloudWatch
# OBSERVABILITY_TRACE=1  -> log de cada etapa do fluxo
OBSERVABILITY_DEBUG=0
OBSERVABILITY_TRACE=0
